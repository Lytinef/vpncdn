name: vpncdn_client
description: VPN-over-NGENIX-CDN клиент (Android, далее Windows/iOS)
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  http: ^1.2.2
  flutter_secure_storage: ^9.2.2
  shared_preferences: ^2.3.2
  webview_flutter: ^4.9.0
  url_launcher: ^6.3.0
  device_info_plus: ^10.1.2
  package_info_plus: ^8.0.2
  app_links: ^6.3.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
